import { ColorfulBorderDirective } from './colorful-border.directive';

describe('ColorfulBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorfulBorderDirective();
    expect(directive).toBeTruthy();
  });
});
